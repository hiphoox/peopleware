<h4>Curriculum: <%= @profile.id %></h4>

<%= render "form.html", changeset: @changeset,
                        action: profile_path(@conn, :update, @profile),
                        states: @states,
                        contractings: @contractings,
                        errors: @errors %>

<%= link "Regresar", to: profile_path(@conn, :index) %>
